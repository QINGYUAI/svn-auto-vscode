# 🚀 快速参考指南

本文档提供常用的命令和操作快速参考。

## 📦 开发命令

### 编译和构建
```bash
# 开发模式编译（带 source map）
npm run compile

# 生产模式编译（优化）
npm run package

# 监听模式编译（自动重新编译）
npm run watch

# 完整构建（包含检查）
npm run build
```

### 代码质量
```bash
# 运行 ESLint 检查
npm run lint

# 运行测试
npm test

# 编译测试文件
npm run compile-tests

# 监听模式编译测试
npm run watch-tests
```

## 📦 打包命令

### 使用脚本（推荐）
```bash
# 构建项目
npm run build

# 打包 VSIX 文件
npm run package:vsix

# 发布到 Marketplace
npm run publish:extension
```

### 使用 vsce 命令
```bash
# 检查 vsce 版本
vsce --version

# 登录（首次使用）
vsce login QINGYUAI

# 打包 VSIX
vsce package

# 打包到指定目录
vsce package -o release/

# 发布（自动打包）
vsce publish

# 发布指定版本
vsce publish 0.1.2

# 自动递增版本并发布
vsce publish patch   # 0.1.1 -> 0.1.2
vsce publish minor   # 0.1.1 -> 0.2.0
vsce publish major   # 0.1.1 -> 1.0.0
```

## 🔧 安装和测试

### 本地安装 VSIX
```bash
# 安装本地 VSIX 文件
code --install-extension svn-git-auto-commit-0.1.1.vsix

# 强制安装（覆盖已安装版本）
code --install-extension svn-git-auto-commit-0.1.1.vsix --force

# 卸载扩展
code --uninstall-extension QINGYUAI.svn-git-auto-commit

# 列出已安装的扩展
code --list-extensions

# 显示扩展详情
code --list-extensions --show-versions | grep svn
```

### 调试模式
```bash
# 在项目目录中启动调试
code .

# 按 F5 启动调试模式
# 会打开新的 VSCode 窗口，加载插件进行测试
```

## 📝 版本管理

### 更新版本号
编辑 `package.json`：
```json
{
  "version": "0.1.2"  // 更新版本号
}
```

### Git 标签
```bash
# 创建版本标签
git tag v0.1.2

# 推送标签到远程
git push origin v0.1.2

# 查看所有标签
git tag

# 删除标签
git tag -d v0.1.2
git push origin :refs/tags/v0.1.2
```

## 🔍 检查命令

### 检查文件
```bash
# 检查 VSIX 文件内容
vsce ls

# 查看文件大小
ls -lh *.vsix

# Windows PowerShell
Get-ChildItem *.vsix | Select-Object Name, Length
```

### 检查配置
```bash
# 检查 package.json 语法
node -e "console.log(JSON.parse(require('fs').readFileSync('package.json')))"

# 检查 TypeScript 配置
npx tsc --showConfig
```

## 📊 发布检查清单

发布前快速检查：

```bash
# 1. 检查代码
npm run lint

# 2. 运行测试
npm test

# 3. 构建项目
npm run build

# 4. 检查版本号
node -e "console.log(require('./package.json').version)"

# 5. 检查 Git 状态
git status

# 6. 打包 VSIX
npm run package:vsix

# 7. 检查 VSIX 文件
vsce ls
```

## 🔗 重要链接

- **插件市场**: https://marketplace.visualstudio.com/items?itemName=QINGYUAI.svn-git-auto-commit
- **管理控制台**: https://marketplace.visualstudio.com/manage/publishers/QINGYUAI/extensions/svn-git-auto-commit/hub
- **GitHub 仓库**: https://github.com/QINGYUAI/svn-auto-vscode
- **问题反馈**: https://github.com/QINGYUAI/svn-auto-vscode/issues

## 📚 相关文档

- [打包发布流程](./打包发布流程.md) - 详细的打包和发布说明
- [项目结构说明](./项目结构说明.md) - 项目结构说明
- [使用文档](./使用文档.md) - 功能使用说明

---

**最后更新**: 2025-11-20

