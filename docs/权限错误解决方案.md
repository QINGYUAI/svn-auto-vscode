# 🔧 权限错误解决方案

本文档专门解决 vsce 登录和发布时的权限错误问题。

## ❌ 常见错误

### 错误 1: Access Denied - View user permissions

**错误信息**：
```
Access Denied: 182e1f5f-62fb-651a-b6b9-5c8aeb260729 needs the following permission(s) on the resource /2321135061 to perform this action: View user permissions on a resource
```

**原因分析**：
- Token 缺少 `User Profile: Read` 权限
- vsce 需要此权限来验证用户身份和权限

**解决方案**：

#### 步骤 1: 访问 Token 管理页面

访问：https://dev.azure.com/2321135061/_usersSettings/tokens

#### 步骤 2: 删除旧 Token（如果存在）

1. 找到现有的 Token
2. 点击删除或撤销

#### 步骤 3: 创建新 Token

1. 点击 **"+ New Token"**

2. 填写信息：
   - **Name**: `VSCode Extension Publishing`
   - **Organization**: `2321135061`
   - **Expiration**: `365 days`（1年）

3. **关键：设置权限**
   - 选择 **"Custom defined"**
   - 展开 **"Scopes"**
   - **必须勾选以下权限**：
     ```
     ☑ Marketplace (Manage)          # 必需：发布扩展
     ☑ User Profile (Read)            # 必需：解决此错误
     ☑ Organization (Read)            # 推荐：查看组织信息
     ```

4. 点击 **"Create"**

5. **立即复制 Token**（只显示一次）

#### 步骤 4: 重新登录

```bash
vsce login 2321135061
# 粘贴新创建的 Token
```

---

### 错误 2: TF400813 - 用户未授权

**错误信息**：
```
TF400813: The user is not authorized to access this resource.
```

**解决方案**：

1. **检查 Publisher 是否存在**：
   - 访问：https://marketplace.visualstudio.com/manage/publishers
   - 如果 `2321135061` 不存在，需要先创建

2. **创建 Publisher**（如果需要）：
   - 访问：https://marketplace.visualstudio.com/manage/create-publisher
   - Publisher ID: `2321135061`
   - Display Name: 你的显示名称
   - 填写其他信息并创建

3. **重新创建 Token**（确保权限正确）：
   - Marketplace: Manage ✅
   - User Profile: Read ✅
   - Organization: Read ✅

---

## 🔍 完整权限检查清单

创建 Token 时，确保勾选：

- [x] **Marketplace: Manage** - 发布和管理扩展（必需）
- [x] **User Profile: Read** - 查看用户信息（必需，解决权限错误）
- [x] **Organization: Read** - 查看组织信息（推荐）

---

## 🚀 快速修复步骤

### 方法 1: 使用 Full Access（最简单）

如果组织允许，创建 Token 时选择 **"Full access"**：

1. 访问 Token 创建页面
2. 选择 **"Full access"**（而不是 Custom defined）
3. 创建 Token
4. 使用新 Token 登录

### 方法 2: 精确权限设置

1. 访问：https://dev.azure.com/2321135061/_usersSettings/tokens
2. 删除旧 Token
3. 创建新 Token，勾选：
   - Marketplace: Manage
   - User Profile: Read
   - Organization: Read
4. 复制 Token
5. 运行：`vsce login 2321135061`
6. 粘贴 Token

---

## ✅ 验证修复

登录成功后，验证：

```bash
# 检查登录状态
vsce ls

# 尝试打包（不发布）
vsce package

# 如果打包成功，说明权限正确
```

---

## 📝 注意事项

1. **Token 安全**：
   - 不要将 Token 提交到 Git
   - 不要分享给他人
   - Token 泄露后立即撤销

2. **权限最小化**：
   - 只授予必要的权限
   - 定期检查 Token 权限

3. **组织名称**：
   - 确保与 `package.json` 中的 `publisher` 一致
   - 区分大小写

---

## 🔗 相关链接

- [Token 设置指南](./Token设置指南.md)
- [打包发布流程](./打包发布流程.md)
- [Azure DevOps Token 管理](https://dev.azure.com/2321135061/_usersSettings/tokens)
- [VSCode Marketplace 管理](https://marketplace.visualstudio.com/manage)

---

**最后更新**: 2025-11-20

